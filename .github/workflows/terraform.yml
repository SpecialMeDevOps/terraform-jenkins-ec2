name: Terraform

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - uses: terraform-linters/setup-tflint@v4
      - name: Terraform fmt
        run: terraform fmt -check -recursive
      - name: Terraform init
        run: terraform init -backend=false -input=false
      - name: Terraform validate
        run: terraform validate
      - name: TFLint
        run: |
          tflint --init
          tflint --format compact
      - name: Checkov
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: .
          framework: terraform
          quiet: true
          # These checks represent deliberate lab-mode choices or controls
          # that require account-wide networking/logging resources not in scope.
          skip_check: CKV_AWS_88,CKV_AWS_130,CKV_AWS_260,CKV_AWS_382,CKV2_AWS_5,CKV2_AWS_11,CKV2_AWS_12
